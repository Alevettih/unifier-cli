import { PathToNamePipe } from './path-to-name.pipe';

describe('PathToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PathToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
